# missSYNC

- 日期：2026-03-17
- 问题：在本 fork 项目中，`bootstrap` 不存在是正常现象，因为当前变更本身就在增加这部分能力；但 `sync` 本应是原本就存在的能力，却在当时环境中缺失。
- 现象：归档前尝试执行 sync 时，相关 skill/命令不可用，导致无法按预期完成 archive 前同步。
- 判断：这说明当前 fork/安装产物与预期能力面不一致，至少需要检查是否遗漏了原版已有的 sync 能力、相关命令产物、技能暴露或安装步骤。
- 后续检查点：
  - 检查 fork 构建/安装结果中是否遗漏 sync 相关命令或技能
  - 检查 slash command / skill generation 是否遗漏 sync 暴露
  - 检查发布产物与源码能力面是否一致
  - 后续做同类能力面变更时，复查还有没有其它“原版已有但 fork 缺失”的内容
- 当前处理：用户已手动重装原版 OpenSpec，当前环境已恢复 `sync` 能力，可继续执行同步与归档。
